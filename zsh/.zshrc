# This file is run whenever a new terminal is opened (e.g., new screen tab).
# It should contain things that must be rerun every time a shell is opened.

BASE_DIR=$HOME/src/dotfiles/zsh
source $BASE_DIR/.zcomp
source $BASE_DIR/.zle
source $BASE_DIR/.zopts
source $BASE_DIR/.zprompt
source $BASE_DIR/.dircolors
source $BASE_DIR/.aliases
